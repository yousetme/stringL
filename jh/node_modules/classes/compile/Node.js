Set.Class('classes.compile.Node', {
    constructor: function(name, file, start, end, line, col, type) {
        this.fn = name;
        this.file = file;
        this.start = start;
        this.end = end;
        this.line = line;
        this.col = col;
        this.type = type || 0;
    }
});
